# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets / local
.env
*.local
.claude/settings.local.json

# Eval data — historical corpora are pointers, never committed (05 §A.2)
eval/corpora/*
!eval/corpora/.gitkeep

# Large data bundles (ATT&CK STIX ~50MB) — fetched via scripts/fetch_attack.py
data/

# Durable checkpointer store
.threat_agents/
